name: Daily Job

on:
  schedule:
    - cron: '0 8 * * *'  # Runs at 8am IST daily
    - cron: '15 20 * * *' # Runs at 8pm IST daily.

jobs:
  run-daily:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a script
        env:
          ENVIRONMENT: QA  # Environment variable
          BROWSER: chrome  # Browser variable
        run: |
          echo "Running daily job"
          echo "Environment: $ENVIRONMENT"
          echo "Browser: $BROWSER"
          # Add your script or commands here
